// -*- C++ -*-
//
// Package:    DummyFilter
// Class:      DummyFilter
// 
/**\class DummyFilter DummyFilter.cc DummyFilter/src/DummyFilter.cc

 Description: [one line class summary]

 Implementation:
     [Notes on implementation]
*/
//
// Original Author:  Yuriy Pakhotin,,,
//         Created:  Fri Sep 13 11:36:21 CDT 2013
// $Id: DummyFilter.cc,v 1.1 2013/09/13 16:44:01 pakhotin Exp $
//
//


// system include files
#include <memory>

// user include files
#include "FWCore/Framework/interface/Frameworkfwd.h"
#include "FWCore/Framework/interface/EDFilter.h"

#include "FWCore/Framework/interface/Event.h"
#include "FWCore/Framework/interface/MakerMacros.h"

#include "FWCore/ParameterSet/interface/ParameterSet.h"

#include "FWCore/Framework/interface/ESHandle.h"
#include "FWCore/Utilities/interface/InputTag.h"

//
// class declaration
//

class DummyFilter : public edm::EDFilter {
  public:
    explicit DummyFilter(const edm::ParameterSet&);
    ~DummyFilter();

  private:
    virtual void beginJob() ;
    virtual bool filter(edm::Event&, const edm::EventSetup&);
    virtual void endJob() ;
      
  // ----------member data ---------------------------
  
  bool m_filterAll;

};

//
// constants, enums and typedefs
//

//
// static data member definitions
//

//
// constructors and destructor
//
DummyFilter::DummyFilter(const edm::ParameterSet& iConfig)
  : m_filterAll(iConfig.getParameter<bool>("filterAll"))
{}


DummyFilter::~DummyFilter()
{
 
   // do anything here that needs to be done at desctruction time
   // (e.g. close files, deallocate resources etc.)

}


//
// member functions
//

// ------------ method called on each new Event  ------------
bool DummyFilter::filter(edm::Event& iEvent, const edm::EventSetup& iSetup)
{

  if (m_filterAll) return true;
  
  return false;
}

// ------------ method called once each job just before starting event loop  ------------
void 
DummyFilter::beginJob()
{
}

// ------------ method called once each job just after ending the event loop  ------------
void 
DummyFilter::endJob() {
}

//define this as a plug-in
DEFINE_FWK_MODULE(DummyFilter);
